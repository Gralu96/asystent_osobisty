DIAGRAM UML - ASYSTENT OSOBISTY

┌───────────────────────────────┐
│    <<abstract>>              │
│    InterfejsUzytkownika      │
├───────────────────────────────┤
│ + wyswietl_menu()            │
│ + pobierz_wybor()            │
│ + wyswietl_zadania()         │
│ + wyswietl_notatki()         │
│ + pobierz_dane_zadania()     │
│ + pobierz_dane_notatki()     │
└─────────────┬─────────────────┘
              ▲
              │
              │
┌─────────────┴─────────────────┐
│      InterfejsKonsolowy       │
├───────────────────────────────┤
│ + wyswietl_menu()            │
│ + pobierz_wybor()            │
│ + wyswietl_zadania()         │
│ + wyswietl_notatki()         │
│ + pobierz_dane_zadania()     │
│ + pobierz_dane_notatki()     │
└───────────────────────────────┘
              ▲
              │
              ◆
┌───────────────────────────────┐
│      AsystentOsobisty        │
├───────────────────────────────┤
│ - interfejs                  │
│ - zadania: List[Dict]        │
│ - notatki: List[Dict]        │
│ - plik_zadania: str          │
│ - plik_notatki: str          │
├───────────────────────────────┤
│ + __init__(interfejs)        │
│ + wczytaj_dane()            │
│ + zapisz_dane()             │
│ + dodaj_zadanie()           │
│ + dodaj_notatke()           │
│ + uruchom()                 │
└───────────────────────────────┘

Legenda:
▲ - dziedziczenie (inheritance)
◆ - kompozycja (composition)
<<abstract>> - klasa abstrakcyjna
+ - metoda publiczna
- - pole/metoda prywatna
